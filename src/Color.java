package src;

/* 
 * Enum used to represent the players in the game
 * Each player is assigned to a color
 * If more colors are added, the game will be able to support more players
 * If wanted to change the game to be used in a different context, the colors can be changed by other names
*/

public enum Color {
    BLACK, RED, GREEN, BLUE;
}